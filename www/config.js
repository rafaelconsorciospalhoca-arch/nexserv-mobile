// App empacotado (Capacitor): roda em origem própria (capacitor://), não no
// domínio do backend como a versão web — precisa de URL absoluta.
window.CHAMA_CONFIG = { apiBase: 'https://www.nexserv.com.br' };
